# Copy this file to .env.local — Next.js loads .env.local, NOT .env.example
# Cloudinary (for product images)
# Cloud name from https://cloudinary.com/console — required for CldImage delivery
# API key/secret are only needed for uploads, not for displaying images
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dxas4fpir

# Folder prefix when assets live under Home > Metal Mart (Media Library)
# Public IDs become "Metal Mart/team_work_makes_the_Dream_work_ljp4we" etc.
NEXT_PUBLIC_CLOUDINARY_FOLDER_PREFIX=
