{
  "name": "shop-agents-catalog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "rm -rf src/shared && cp -r ../shared src/shared",
    "prebuild": "rm -rf src/shared && cp -r ../shared src/shared",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "npm run prebuild && tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.4",
    "@types/node": "^20",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  }
}
