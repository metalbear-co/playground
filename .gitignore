apps/ip-visit/ip-visit-sqs-consumer/ip-visit-sqs-consumer
**/.DS_Store

# The localstack helm charts get cloned when creating the localstack setup.
overlays/localstack/charts

# Helm charts that are used in base (like kafka) get cloned there when installing the chart.
base/charts

.idea
.vscode/*
!.vscode/launch.json

# Local env (secrets, API keys)
**/.env.local
**/.env*.local

# Local-only minikube shop script (contains mirrord license key; do not commit)
apps/shop/scripts/setup-minikube-shop.sh

# Node dependencies
**/node_modules

# Next.js build output
**/.next

# Generated TypeScript output (server build)
apps/visualization/visualization-backend/dist/
apps/shop/*/dist/