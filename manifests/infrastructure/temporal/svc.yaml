apiVersion: v1
kind: Service
metadata:
  name: temporal
  labels:
    app: temporal
spec:
  type: ClusterIP
  ports:
    - port: 7233
      name: frontend
      protocol: TCP
      targetPort: 7233
  selector:
    app: temporal
