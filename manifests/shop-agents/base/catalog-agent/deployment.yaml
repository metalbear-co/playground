apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-agent
  template:
    metadata:
      labels:
        app: catalog-agent
    spec:
      containers:
        - name: catalog-agent
          image: ghcr.io/metalbear-co/playground-shop-agents-catalog:latest
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "80"
            - name: INVENTORY_SERVICE_URL
              value: "http://inventory-service.shop.svc.cluster.local"
            - name: PUBLIC_URL
              value: "http://catalog-agent.shop-agents.svc.cluster.local"
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /health
              port: 80
          livenessProbe:
            httpGet:
              path: /health
              port: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
