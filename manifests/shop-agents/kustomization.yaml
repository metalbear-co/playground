apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: shop-agents

resources:
  - ./base/router-agent
  - ./base/order-agent
  - ./base/support-frontend

commonLabels:
  env: playground
  team: shop-agents
