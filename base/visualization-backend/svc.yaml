apiVersion: v1
kind: Service
metadata:
  name: visualization-backend
  labels:
    app: visualization-backend
spec:
  selector:
    app: visualization-backend
  ports:
    - name: http
      port: 80
      targetPort: http
