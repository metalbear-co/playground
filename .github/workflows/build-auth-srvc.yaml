name: Release auth-srvc
on:
  push:
    branches: [main]
    paths:
      - "apps/ci-lab/auth-srvc/**"
      - ".github/workflows/build-auth-srvc.yaml"
  workflow_dispatch:

jobs:
  image:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          file: apps/ci-lab/auth-srvc/Dockerfile
          platforms: linux/amd64
          context: apps/ci-lab/auth-srvc
          push: true
          tags: |
            ghcr.io/metalbear-co/playground-auth-srvc:latest
            ghcr.io/metalbear-co/playground-auth-srvc:${{ github.sha }}
